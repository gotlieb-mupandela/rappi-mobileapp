# Optional — the Expo app ships a bundled catalog and local auth.
# Set these only if you later wire the same Supabase project as rappi-webapp.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
